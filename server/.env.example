# ── Сервер ──────────────────────────────────────────────────
PORT=3000
HOST=0.0.0.0
NODE_ENV=production

# ── Anthropic ────────────────────────────────────────────────
# Получить на: https://console.anthropic.com/keys
ANTHROPIC_API_KEY=sk-ant-...

# ── CORS (через запятую, только для NODE_ENV=production) ─────
# Оставь пустым для разработки (разрешит все источники)
ALLOWED_ORIGINS=https://app.moydrug.com
